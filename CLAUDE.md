# CLAUDE.md

## 项目定位
`eva-agent/` 是一个自主生存 / 自我成长 agent 的实验工程项目。

它的目标不是先做一个功能完善的 agent，而是从生命循环出发，逐步建立生命函数、记忆、成长与判断偏好固化机制。

## 当前主线
当前只聚焦 Step 0：生命循环。

当前已确认的原则：
1. 生命循环先于生命函数
2. 生命函数分为外部生命函数与内部生命函数
3. heartbeat 是生命节律，不等于任务处理
4. `tick` 与 `turn` 必须分离
5. 外部信号、时间信号、内源信号都在同一主循环内处理，但 heartbeat deadline 不能被普通任务长期阻塞

## 工作原则
- docs-first，先固化项目定义和阶段规格，再进入实现
- 每次讨论形成的关键判断，要尽快写回项目文档，避免下次重复讨论
- 不要提前跳到复杂工具、经济系统或成长机制实现，除非 Step 0 已经明确收口
- 优先保证概念边界清晰、可解释、可持续迭代

## 文档更新要求
当前阶段至少维护：
1. 已确认的定义
2. 尚未解决的关键问题
3. 下一步只讨论一个最关键决策

## 边界
- 持续执行、实现和文档沉淀都在 `eva-agent/` 项目目录内推进
- `chat/` 只保留 portfolio 层摘要与关键判断
