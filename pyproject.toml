[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "eva-agent"
version = "0.1.0"
description = "Step 0 lifecycle runtime for eva-agent"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["eva"]
